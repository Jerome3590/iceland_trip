.DS_Store
Thumbs.db
Desktop.ini
*.log
.env
.env.*
.localdev
node_modules/
iceland-route-map/

# Photos — originals and converted web cache (large binary files)
photos/
photos-web/

# Local only — will migrate to private repo
LESSONS_LEARNED.md

# Generated pipeline outputs
photo-gps.csv
photo-stops.json
route-data-photos.json
unmatched.json
s3-manifest.txt

# Archive — applied one-offs, not needed in active workspace
# (kept in repo history via git, not deleted)
# archive/ is committed but excluded from active tracking concerns
